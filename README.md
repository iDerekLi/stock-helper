# stock-widget-chrome-extension

股票小组件-谷歌扩展

扩展程序: `stock-widget.crx`

## 离线安装

方法一: 拖拽安装

1. 输入网址 `chrome://extensions/`
2. 直接拖拽安装

方法二: 无法直接离线安装的浏览器

1. 需要把 `stock-widget.crx` 后缀 `.crx` 更改为 `.zip` 并解压缩。
2. 输入网址 `chrome://extensions/` => 开启 `开发者模式` -> 点击 `加载已解压的扩展程序`

